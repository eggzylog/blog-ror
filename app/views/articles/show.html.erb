<%= link_to "Home", root_path %>

<h1><%= @article.title %></h1>
<p><%= @article.body %></p>

<ul>
  <li>
    <%= link_to "Edit", edit_article_path(@article) %>
  </li>
  <li>
    <%= link_to "Delete", article_path(@article), data: {
      turbo_method: :delete,
      turbo_confirm: "Are you sure?"
    } %>
  </li>
</ul>

<%# DISPLAYING COMMENTS %>
<h2>Comments:</h2>
<%= render @article.comments %>

<%# NEW FORM FOR ADDING A COMMENT %>
<h2>Add a comment:</h2>
<%= render "comments/form" %>
